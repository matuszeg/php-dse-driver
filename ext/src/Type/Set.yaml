---
Dse\Type\Set:
  comment: ""
  methods:
    name:
      comment: Returns "set"
      return:
        comment: '"set"'
        type: string
    __toString:
      comment: Returns type representation in CQL, e.g. `set<varchar>`
      return:
        comment: Type representation in CQL
        type: string
    valueType:
      comment: Returns type of values
      return:
        comment: Type of values
        type: Type
    create:
      comment: |-
        Creates a new Dse\Set from the given values.

        @throws Exception\InvalidArgumentException when values given are of a
                                                   different type than what this
                                                   set type expects.

                                 no values are given, creates an empty set.
      params:
        value:
          comment: ',... One or more values to be added to the set. When'
          type: mixed
      return:
        comment: '   A set with given values.'
        type: Dse\Set
...
