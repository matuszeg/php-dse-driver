---
Graph\FutureResultSet:
  comment: |
    A future result set of an asynchronously running graph query.

    @see Session::executeGraphAsync()
  methods:
    get:
      comment: |
        Get the result set of an asynchronously executed graph query.

        @throws Exception
      params:
        timeout:
          comment: The amount of time to wait for the graph query to return in seconds.
          type: double
      return:
        comment: A result set of arbitrary graph results.
        type: Graph\ResultSet
...
